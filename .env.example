# Azure DevOps MCP Server Configuration

# Your Azure DevOps organization name (e.g., 'contoso')
# This is the part before .visualstudio.com or in dev.azure.com/YOUR_ORG
ADO_ORGANIZATION=your-org-name

# Personal Access Token for authentication
# Generate at: https://dev.azure.com/YOUR_ORG/_usersSettings/tokens
# Required scopes: Code (Read), Work Items (Read & Write), Build (Read), Release (Read)
ADO_PAT=your-personal-access-token-here

# Authentication type (default: env)
# Options: env, envvar, azcli
# Note: 'interactive' is not supported in Docker
# Use 'env' or 'envvar' for PAT-based authentication
ADO_AUTH_TYPE=env

# Port for the HTTP/SSE wrapper (default: 8055)
ADO_MCP_PORT=8055

# Log level (default: info)
# Options: debug, info, warn, error
LOG_LEVEL=info

# Optional: Path to vibes workspace (if integrating with vibes)
VIBES_PATH=/workspace/vibes
